From cc13b5c88e69051245cc18da3e67e7a558b18983 Mon Sep 17 00:00:00 2001
From: Saksham Mittal <saksham1991999@gmail.com>
Date: Fri, 22 Aug 2025 20:15:22 +0530
Subject: [PATCH 1/6] Refactor API base URL handling to ensure HTTPS is used in
 production for both main and property API services.

---
 src/services/api.js                | 12 +++++++++++-
 src/services/propertyAPIService.js | 12 +++++++++++-
 2 files changed, 22 insertions(+), 2 deletions(-)

diff --git a/src/services/api.js b/src/services/api.js
index a3bfaa3..0dfd0f4 100644
--- a/src/services/api.js
+++ b/src/services/api.js
@@ -1,8 +1,18 @@
 import axios from 'axios';
 
+// Ensure the API URL always uses HTTPS in production
+const getApiBaseUrl = () => {
+  const url = import.meta.env.VITE_API_BASE_URL || 'https://360ghar.up.railway.app/api/v1';
+  // Force HTTPS in production
+  if (typeof window !== 'undefined' && window.location.protocol === 'https:' && url.startsWith('http://')) {
+    return url.replace('http://', 'https://');
+  }
+  return url;
+};
+
 // Create axios instance with base URL
 const api = axios.create({
-  baseURL: import.meta.env.VITE_API_BASE_URL,
+  baseURL: getApiBaseUrl(),
   headers: {
     'Content-Type': 'application/json',
   },
diff --git a/src/services/propertyAPIService.js b/src/services/propertyAPIService.js
index 811c81f..f6ce17c 100644
--- a/src/services/propertyAPIService.js
+++ b/src/services/propertyAPIService.js
@@ -1,8 +1,18 @@
 import axios from 'axios';
 
+// Ensure the API URL always uses HTTPS in production
+const getApiBaseUrl = () => {
+  const url = import.meta.env.VITE_API_BASE_URL || 'https://360ghar.up.railway.app/api/v1';
+  // Force HTTPS in production
+  if (typeof window !== 'undefined' && window.location.protocol === 'https:' && url.startsWith('http://')) {
+    return url.replace('http://', 'https://');
+  }
+  return url;
+};
+
 // Create a separate axios instance for public property endpoints (no auth required)
 const publicApi = axios.create({
-  baseURL: import.meta.env.VITE_API_BASE_URL,
+  baseURL: getApiBaseUrl(),
   headers: {
     'Content-Type': 'application/json',
   },
-- 
2.39.5 (Apple Git-154)


From 6b691904dcaebc78535d550b612c22c8e7f7ddf4 Mon Sep 17 00:00:00 2001
From: Saksham Mittal <saksham1991999@gmail.com>
Date: Fri, 22 Aug 2025 20:35:36 +0530
Subject: [PATCH 2/6] Enhance API base URL handling with debug logging and
 improved HTTPS enforcement in both main and property API services.

---
 src/services/api.js                | 32 +++++++++++++++++++++++++-----
 src/services/propertyAPIService.js | 18 ++++++++++++-----
 2 files changed, 40 insertions(+), 10 deletions(-)

diff --git a/src/services/api.js b/src/services/api.js
index 0dfd0f4..9566544 100644
--- a/src/services/api.js
+++ b/src/services/api.js
@@ -2,12 +2,34 @@ import axios from 'axios';
 
 // Ensure the API URL always uses HTTPS in production
 const getApiBaseUrl = () => {
-  const url = import.meta.env.VITE_API_BASE_URL || 'https://360ghar.up.railway.app/api/v1';
-  // Force HTTPS in production
-  if (typeof window !== 'undefined' && window.location.protocol === 'https:' && url.startsWith('http://')) {
-    return url.replace('http://', 'https://');
+  const envUrl = import.meta.env.VITE_API_BASE_URL;
+
+  // Debug logging for environment variable (remove in production)
+  if (typeof window !== 'undefined') {
+    console.log('🔍 API Base URL Debug:', {
+      envUrl,
+      currentProtocol: window.location.protocol,
+      isHttpsPage: window.location.protocol === 'https:'
+    });
   }
-  return url;
+
+  // If environment variable is set, use it and ensure HTTPS
+  if (envUrl) {
+    // Always force HTTPS in production or if the current page is HTTPS
+    if (typeof window !== 'undefined' && window.location.protocol === 'https:') {
+      const finalUrl = envUrl.startsWith('https://') ? envUrl : envUrl.replace('http://', 'https://');
+      console.log('🔒 Final API URL (HTTPS enforced):', finalUrl);
+      return finalUrl;
+    }
+    // For development or non-HTTPS environments, use as-is
+    console.log('📝 Using API URL as-is:', envUrl);
+    return envUrl;
+  }
+
+  // Fallback URL with HTTPS
+  const fallbackUrl = 'https://360ghar.up.railway.app/api/v1';
+  console.log('⚠️ Using fallback API URL:', fallbackUrl);
+  return fallbackUrl;
 };
 
 // Create axios instance with base URL
diff --git a/src/services/propertyAPIService.js b/src/services/propertyAPIService.js
index f6ce17c..91a42a3 100644
--- a/src/services/propertyAPIService.js
+++ b/src/services/propertyAPIService.js
@@ -2,12 +2,20 @@ import axios from 'axios';
 
 // Ensure the API URL always uses HTTPS in production
 const getApiBaseUrl = () => {
-  const url = import.meta.env.VITE_API_BASE_URL || 'https://360ghar.up.railway.app/api/v1';
-  // Force HTTPS in production
-  if (typeof window !== 'undefined' && window.location.protocol === 'https:' && url.startsWith('http://')) {
-    return url.replace('http://', 'https://');
+  const envUrl = import.meta.env.VITE_API_BASE_URL;
+
+  // If environment variable is set, use it and ensure HTTPS
+  if (envUrl) {
+    // Always force HTTPS in production or if the current page is HTTPS
+    if (typeof window !== 'undefined' && window.location.protocol === 'https:') {
+      return envUrl.startsWith('https://') ? envUrl : envUrl.replace('http://', 'https://');
+    }
+    // For development or non-HTTPS environments, use as-is
+    return envUrl;
   }
-  return url;
+
+  // Fallback URL with HTTPS
+  return 'https://360ghar.up.railway.app/api/v1';
 };
 
 // Create a separate axios instance for public property endpoints (no auth required)
-- 
2.39.5 (Apple Git-154)


From 190feb5a4b5bafc86087b2260f119fe49f599407 Mon Sep 17 00:00:00 2001
From: Saksham Mittal <saksham1991999@gmail.com>
Date: Fri, 22 Aug 2025 20:41:48 +0530
Subject: [PATCH 3/6] Add debug logging for API requests in main and property
 services

---
 src/services/api.js                | 18 ++++++++++++++++++
 src/services/propertyAPIService.js | 21 +++++++++++++++++++++
 2 files changed, 39 insertions(+)

diff --git a/src/services/api.js b/src/services/api.js
index 9566544..b2ed70c 100644
--- a/src/services/api.js
+++ b/src/services/api.js
@@ -40,6 +40,24 @@ const api = axios.create({
   },
 });
 
+// Add request interceptor to log all API calls
+api.interceptors.request.use(
+  (config) => {
+    console.log('🚀 Main API Request Debug:', {
+      method: config.method?.toUpperCase(),
+      url: config.url,
+      baseURL: config.baseURL,
+      fullUrl: `${config.baseURL}${config.url}`,
+      isHttps: config.baseURL?.startsWith('https://')
+    });
+    return config;
+  },
+  (error) => {
+    console.error('❌ Main API Request Error:', error);
+    return Promise.reject(error);
+  }
+);
+
 // Add a request interceptor to attach authentication token
 api.interceptors.request.use(
   (config) => {
diff --git a/src/services/propertyAPIService.js b/src/services/propertyAPIService.js
index 91a42a3..2009f67 100644
--- a/src/services/propertyAPIService.js
+++ b/src/services/propertyAPIService.js
@@ -121,6 +121,13 @@ function buildSearchParams(filters = {}, page = 1, limit = 12) {
  */
 const searchProperties = (filters = {}, page = 1, limit = 12) => {
   const params = buildSearchParams(filters, page, limit);
+  const fullUrl = `${publicApi.defaults.baseURL}/properties?${params.toString()}`;
+  console.log('🔍 Property API Search Debug:', {
+    baseURL: publicApi.defaults.baseURL,
+    params: params.toString(),
+    fullUrl,
+    isHttps: fullUrl.startsWith('https://')
+  });
   return publicApi.get(`/properties?${params.toString()}`);
 };
 
@@ -132,6 +139,13 @@ const searchProperties = (filters = {}, page = 1, limit = 12) => {
  * @returns {Promise<object>} The property data.
  */
 const getPropertyById = (propertyId) => {
+  const fullUrl = `${publicApi.defaults.baseURL}/properties/${propertyId}`;
+  console.log('🔍 Property API GetById Debug:', {
+    baseURL: publicApi.defaults.baseURL,
+    propertyId,
+    fullUrl,
+    isHttps: fullUrl.startsWith('https://')
+  });
   return publicApi.get(`/properties/${propertyId}`);
 };
 
@@ -143,6 +157,13 @@ const getPropertyById = (propertyId) => {
  * @returns {Promise<array>} A list of recommended properties.
  */
 const getRecommendations = (limit = 6) => {
+  const fullUrl = `${publicApi.defaults.baseURL}/properties/recommendations?limit=${limit}`;
+  console.log('🔍 Property API Recommendations Debug:', {
+    baseURL: publicApi.defaults.baseURL,
+    limit,
+    fullUrl,
+    isHttps: fullUrl.startsWith('https://')
+  });
   return publicApi.get(`/properties/recommendations?limit=${limit}`);
 };
 
-- 
2.39.5 (Apple Git-154)


From bf34a9c972a44f8dc31323b868118a83e5a5465f Mon Sep 17 00:00:00 2001
From: Saksham Mittal <saksham1991999@gmail.com>
Date: Fri, 22 Aug 2025 20:45:32 +0530
Subject: [PATCH 4/6] Add HTTPS enforcement interceptors and enhanced debugging
 for API requests in main and property services

---
 src/services/api.js                | 39 +++++++++++++++++++++++++
 src/services/propertyAPIService.js | 46 ++++++++++++++++++++++++++++--
 2 files changed, 83 insertions(+), 2 deletions(-)

diff --git a/src/services/api.js b/src/services/api.js
index b2ed70c..f058c17 100644
--- a/src/services/api.js
+++ b/src/services/api.js
@@ -40,6 +40,31 @@ const api = axios.create({
   },
 });
 
+// Add HTTPS enforcement interceptor
+api.interceptors.request.use(
+  (config) => {
+    // Double-check that we're using HTTPS
+    if (config.baseURL && config.baseURL.startsWith('http://')) {
+      console.error('🚨 HTTP URL detected in main API! Forcing HTTPS...', {
+        originalUrl: config.baseURL,
+        fixedUrl: config.baseURL.replace('http://', 'https://')
+      });
+      config.baseURL = config.baseURL.replace('http://', 'https://');
+    }
+
+    if (config.url && config.url.startsWith('http://')) {
+      console.error('🚨 HTTP URL in main API request URL! Forcing HTTPS...', {
+        originalUrl: config.url,
+        fixedUrl: config.url.replace('http://', 'https://')
+      });
+      config.url = config.url.replace('http://', 'https://');
+    }
+
+    return config;
+  },
+  (error) => Promise.reject(error)
+);
+
 // Add request interceptor to log all API calls
 api.interceptors.request.use(
   (config) => {
@@ -65,6 +90,20 @@ api.interceptors.request.use(
     if (token) {
       config.headers.Authorization = `Bearer ${token}`;
     }
+
+    // Enhanced debugging for main API service
+    console.log('🔧 Main API Request Interceptor Debug:', {
+      url: config.url,
+      baseURL: config.baseURL,
+      fullUrl: `${config.baseURL}${config.url}`,
+      method: config.method?.toUpperCase(),
+      isHttps: `${config.baseURL}${config.url}`.startsWith('https://'),
+      hasAuth: !!token,
+      userAgent: typeof navigator !== 'undefined' ? navigator.userAgent : 'unknown',
+      currentLocation: typeof window !== 'undefined' ? window.location.href : 'unknown',
+      protocol: typeof window !== 'undefined' ? window.location.protocol : 'unknown'
+    });
+
     return config;
   },
   (error) => Promise.reject(error)
diff --git a/src/services/propertyAPIService.js b/src/services/propertyAPIService.js
index 2009f67..83cd32e 100644
--- a/src/services/propertyAPIService.js
+++ b/src/services/propertyAPIService.js
@@ -26,6 +26,31 @@ const publicApi = axios.create({
   },
 });
 
+// Add request interceptor to enforce HTTPS on all requests
+publicApi.interceptors.request.use(
+  (config) => {
+    // Double-check that we're using HTTPS
+    if (config.baseURL && config.baseURL.startsWith('http://')) {
+      console.error('🚨 HTTP URL detected in property API! Forcing HTTPS...', {
+        originalUrl: config.baseURL,
+        fixedUrl: config.baseURL.replace('http://', 'https://')
+      });
+      config.baseURL = config.baseURL.replace('http://', 'https://');
+    }
+
+    if (config.url && config.url.startsWith('http://')) {
+      console.error('🚨 HTTP URL in request URL! Forcing HTTPS...', {
+        originalUrl: config.url,
+        fixedUrl: config.url.replace('http://', 'https://')
+      });
+      config.url = config.url.replace('http://', 'https://');
+    }
+
+    return config;
+  },
+  (error) => Promise.reject(error)
+);
+
 // Simple error handling for public endpoints - no auth redirects
 publicApi.interceptors.response.use(
   (response) => response,
@@ -126,9 +151,26 @@ const searchProperties = (filters = {}, page = 1, limit = 12) => {
     baseURL: publicApi.defaults.baseURL,
     params: params.toString(),
     fullUrl,
-    isHttps: fullUrl.startsWith('https://')
+    isHttps: fullUrl.startsWith('https://'),
+    userAgent: navigator.userAgent,
+    currentLocation: window.location.href,
+    protocol: window.location.protocol
   });
-  return publicApi.get(`/properties?${params.toString()}`);
+
+  // Add request interceptor for this specific call
+  const originalRequest = publicApi.get;
+  const interceptedRequest = (url, config) => {
+    console.log('🚀 INTERCEPTED Property API Request:', {
+      url,
+      baseURL: publicApi.defaults.baseURL,
+      fullUrl: `${publicApi.defaults.baseURL}${url}`,
+      method: 'GET',
+      isHttps: `${publicApi.defaults.baseURL}${url}`.startsWith('https://')
+    });
+    return originalRequest.call(publicApi, url, config);
+  };
+
+  return interceptedRequest(`/properties?${params.toString()}`);
 };
 
 /**
-- 
2.39.5 (Apple Git-154)


From 871a9c7e0bc28eb990d60beb8ecec3bf50a1d489 Mon Sep 17 00:00:00 2001
From: Saksham Mittal <saksham1991999@gmail.com>
Date: Fri, 22 Aug 2025 20:48:44 +0530
Subject: [PATCH 5/6] Refactor property fetching methods in PropertyFilter and
 PropertyList components to use fetchProperties instead of getAllProperties

---
 src/components/items/PropertyFilter.jsx |  4 ++--
 src/components/items/PropertyList.jsx   | 14 +++++++-------
 2 files changed, 9 insertions(+), 9 deletions(-)

diff --git a/src/components/items/PropertyFilter.jsx b/src/components/items/PropertyFilter.jsx
index 59a8960..0d692d6 100644
--- a/src/components/items/PropertyFilter.jsx
+++ b/src/components/items/PropertyFilter.jsx
@@ -3,7 +3,7 @@ import { Formik, Form, Field } from 'formik';
 import { usePropertyStore } from '../../store';
 
 const PropertyFilter = ({ onFilter }) => {
-  const { setFilters, getAllProperties } = usePropertyStore();
+  const { setFilters, fetchProperties } = usePropertyStore();
 
   const propertyTypes = [
     { value: '', label: 'Any Type' },
@@ -57,7 +57,7 @@ const PropertyFilter = ({ onFilter }) => {
     setFilters(filters);
     
     // Fetch properties with new filters
-    await getAllProperties(filters);
+    await fetchProperties(filters);
     
     // Call onFilter callback if provided
     if (onFilter) {
diff --git a/src/components/items/PropertyList.jsx b/src/components/items/PropertyList.jsx
index afcbd2b..254b6dd 100644
--- a/src/components/items/PropertyList.jsx
+++ b/src/components/items/PropertyList.jsx
@@ -3,18 +3,18 @@ import { Link } from 'react-router-dom';
 import { usePropertyStore } from '../../store';
 
 const PropertyList = ({ limit, filters = {} }) => {
-  const { properties, isLoading, error, getAllProperties } = usePropertyStore();
-  
+  const { properties, isLoading, error, fetchProperties } = usePropertyStore();
+
   useEffect(() => {
-    const fetchProperties = async () => {
-      await getAllProperties({
+    const loadProperties = async () => {
+      await fetchProperties({
         limit: limit || 12,
         ...filters
       });
     };
-    
-    fetchProperties();
-  }, [getAllProperties, limit, filters]);
+
+    loadProperties();
+  }, [fetchProperties, limit, filters]);
   
   if (isLoading) {
     return <div className="text-center py-5">Loading properties...</div>;
-- 
2.39.5 (Apple Git-154)


From a44703a51c122093ffa42ed19dd74f52ba29a8ab Mon Sep 17 00:00:00 2001
From: Saksham Mittal <saksham1991999@gmail.com>
Date: Fri, 22 Aug 2025 20:55:08 +0530
Subject: [PATCH 6/6] Add HTTP request monitoring and enforce HTTPS for fetch
 and XMLHttpRequest in main.jsx; update property API service endpoints to
 ensure trailing slashes for consistency.

---
 src/main.jsx                       | 27 +++++++++++++++++++++++++++
 src/services/api.js                |  7 +++++--
 src/services/propertyAPIService.js |  8 ++++----
 src/services/propertyService.js    | 10 +++++-----
 4 files changed, 41 insertions(+), 11 deletions(-)

diff --git a/src/main.jsx b/src/main.jsx
index 080a59d..076c070 100644
--- a/src/main.jsx
+++ b/src/main.jsx
@@ -9,6 +9,33 @@ import ScrollHideProvider from './contextApi/ScrollHideContext.jsx'
 import BlogDataProvider from './contextApi/BlogDataContext.jsx'
 import { HelmetProvider } from 'react-helmet-async'
 
+// Global HTTP request monitoring to catch any HTTP requests
+if (typeof window !== 'undefined') {
+  const originalFetch = window.fetch;
+  window.fetch = function(...args) {
+    const url = args[0];
+    if (typeof url === 'string' && url.startsWith('http://')) {
+      console.error('🚨 HTTP REQUEST DETECTED VIA FETCH:', url);
+      // Force HTTPS
+      args[0] = url.replace('http://', 'https://');
+      console.log('🔒 FORCED TO HTTPS:', args[0]);
+    }
+    return originalFetch.apply(this, args);
+  };
+
+  // Monitor XMLHttpRequest
+  const originalOpen = XMLHttpRequest.prototype.open;
+  XMLHttpRequest.prototype.open = function(method, url, ...rest) {
+    if (typeof url === 'string' && url.startsWith('http://')) {
+      console.error('🚨 HTTP REQUEST DETECTED VIA XMLHttpRequest:', url);
+      // Force HTTPS
+      url = url.replace('http://', 'https://');
+      console.log('🔒 FORCED TO HTTPS:', url);
+    }
+    return originalOpen.call(this, method, url, ...rest);
+  };
+}
+
 ReactDOM.createRoot(document.getElementById('root')).render(
   <BlogDataProvider>
     <HelmetProvider>
diff --git a/src/services/api.js b/src/services/api.js
index f058c17..80c9bfa 100644
--- a/src/services/api.js
+++ b/src/services/api.js
@@ -9,7 +9,10 @@ const getApiBaseUrl = () => {
     console.log('🔍 API Base URL Debug:', {
       envUrl,
       currentProtocol: window.location.protocol,
-      isHttpsPage: window.location.protocol === 'https:'
+      isHttpsPage: window.location.protocol === 'https:',
+      hostname: window.location.hostname,
+      fullUrl: window.location.href,
+      userAgent: window.navigator.userAgent
     });
   }
 
@@ -116,7 +119,7 @@ api.interceptors.response.use(
     // Handle 401 Unauthorized errors only for authenticated endpoints
     if (error.response && error.response.status === 401) {
       // Check if this is a public endpoint (property viewing)
-      const publicEndpoints = ['/properties?', '/properties/', '/properties/recommendations'];
+      const publicEndpoints = ['/?', '/properties/', '/properties/recommendations/'];
       const isPublicEndpoint = publicEndpoints.some(endpoint => 
         error.config?.url?.includes(endpoint)
       );
diff --git a/src/services/propertyAPIService.js b/src/services/propertyAPIService.js
index 83cd32e..33c7102 100644
--- a/src/services/propertyAPIService.js
+++ b/src/services/propertyAPIService.js
@@ -61,7 +61,7 @@ publicApi.interceptors.response.use(
   }
 );
 
-// Map frontend filters to backend query params for GET /properties
+// Map frontend filters to backend query params for GET /properties/
 function buildSearchParams(filters = {}, page = 1, limit = 12) {
   const params = new URLSearchParams();
   params.set('page', String(page));
@@ -141,12 +141,12 @@ function buildSearchParams(filters = {}, page = 1, limit = 12) {
 
 /**
  * Searches for properties based on filters, location, and pagination.
- * Corresponds to: GET /properties
+ * Corresponds to: GET /properties/
  * NO AUTHENTICATION REQUIRED
  */
 const searchProperties = (filters = {}, page = 1, limit = 12) => {
   const params = buildSearchParams(filters, page, limit);
-  const fullUrl = `${publicApi.defaults.baseURL}/properties?${params.toString()}`;
+  const fullUrl = `${publicApi.defaults.baseURL}/properties/?${params.toString()}`;
   console.log('🔍 Property API Search Debug:', {
     baseURL: publicApi.defaults.baseURL,
     params: params.toString(),
@@ -170,7 +170,7 @@ const searchProperties = (filters = {}, page = 1, limit = 12) => {
     return originalRequest.call(publicApi, url, config);
   };
 
-  return interceptedRequest(`/properties?${params.toString()}`);
+  return interceptedRequest(`/properties/?${params.toString()}`);
 };
 
 /**
diff --git a/src/services/propertyService.js b/src/services/propertyService.js
index 830ab8e..c67d8c3 100644
--- a/src/services/propertyService.js
+++ b/src/services/propertyService.js
@@ -3,7 +3,7 @@ import api from './api';
 export const propertyService = {
   // Get all public properties with optional filters
   getAllProperties: async (filters = {}) => {
-    const response = await api.get('/properties', { params: filters });
+    const response = await api.get('/properties/', { params: filters });
     return response.data;
   },
   
@@ -15,7 +15,7 @@ export const propertyService = {
   
   // Get properties belonging to the current user
   getUserProperties: async (params = {}) => {
-    const response = await api.get('/properties/me', { params });
+    const response = await api.get('/properties/me/', { params });
     return response.data;
   },
   
@@ -27,7 +27,7 @@ export const propertyService = {
   
   // Create new property
   createProperty: async (propertyData) => {
-    const response = await api.post('/properties', propertyData);
+    const response = await api.post('/properties/', propertyData);
     return response.data;
   },
   
@@ -39,13 +39,13 @@ export const propertyService = {
   
   // Delete property
   deleteProperty: async (id) => {
-    const response = await api.delete(`/properties/${id}`);
+    const response = await api.delete(`/properties/${id}/`);
     return response.data;
   },
   
   // Verify property (admin only)
   verifyProperty: async (id) => {
-    const response = await api.put(`/properties/${id}/verify`);
+    const response = await api.put(`/properties/${id}/verify/`);
     return response.data;
   }
 };
-- 
2.39.5 (Apple Git-154)

